# morningtideservices

Static website for Morningtide Services LLC.

## View locally

- Open `index.html` directly in your browser, or
- Run a simple local server:

```bash
cd /workspace
python3 -m http.server 8080
```

Then visit `http://localhost:8080` and open `index.html`.
